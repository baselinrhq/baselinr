# Dagster workspace configuration for ProfileMesh

load_from:
  - python_file:
      relative_path: /app/examples/dagster_repository.py
      working_directory: /app
      location_name: profilemesh

