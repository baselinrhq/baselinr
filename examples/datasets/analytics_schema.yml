schema: analytics
profiling:
  partition:
    key: date
    strategy: latest
    metadata_fallback: true
  sampling:
    enabled: true
    method: random
    fraction: 0.05
drift:
  strategy: absolute_threshold
  absolute_threshold:
    low_threshold: 3.0
    high_threshold: 25.0
    medium_threshold: 10.0
